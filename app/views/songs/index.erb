<!DOCTYPE html>
<html>
  <head>
    <title></title>
  </head>

  <body>
    <h1>Songs</h1>
    <% @songs.each do |song| %>
      <a href=<%="/songs/#{song.slug}"%>><%= song.name %></a><br>
    <% end %>
  </body>
</html>
