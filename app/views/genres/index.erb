<!DOCTYPE html>
<html>
  <head>
    <title></title>
  </head>

  <body>
    <h1>Genres</h1>
    <% @genres.each do |genre| %>
    <a href=<%="/genres/#{genre.slug}"%>><%= genre.name %></a><br>
    <% end %>
  </body>
</html>
