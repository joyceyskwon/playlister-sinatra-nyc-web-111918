<!DOCTYPE html>
<html>
  <head>
    <title></title>
  </head>

  <body>
    <h1>Artists</h1>
    <% @artists.each do |artist| %>
      <a href=<%="/artists/#{artist.slug}"%>><%= artist.name %></a><br>
    <% end %>
  </body>
</html>
